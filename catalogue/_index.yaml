# Registry of L1 catalogue files. Lint enforces that every file in catalogue/
# (other than this index) is listed here, and every entry resolves to an
# existing file. This makes build order deterministic and catches stray files.
files:
  - L1-production-management.yaml
  - L1-customer-management.yaml
